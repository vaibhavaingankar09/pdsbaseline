import React from 'react';

const baseline = (props) => {
    return (
        <div>Metrics Baseline Content
            <ul>
                <li>add/remove project(s)</li>
            </ul>
        </div>
    )
}

export default baseline;